# circuit.config.yaml — Generated by /circuit:setup on 2026-03-29
# Capacitor: Rust core + SwiftUI app
# Customize skills per circuit. Explicit --skills flag always overrides.

circuits:
  develop:
    skills: [tdd, swift-apps]
  decide:
    skills: [architecture-exploration, solution-explorer]
  harden-spec:
    skills: [architecture-exploration, clean-architecture]
  repair-flow:
    skills: [tdd, exhaustive-systems-analysis]
  migrate:
    skills: [audit-and-migrate, tdd]
  ratchet-quality:
    skills: [clean-architecture, tdd]
  cleanup:
    skills: [dead-code-sweep]

dispatch:
  engine: codex
